var NumericFormattedValidator = function (value, callback) {
    "use strict";
    /*		
        		At this spot, you can implement our validations.
		 		I call 'callback(true)' because I want handsontable accepts formatted values as cell entries. 
        */
    callback(true);
};